//! Module for loading state from a file, as in "save/load" load.
//! Saving isn't implemented yet, though.

use bevy::prelude::*;

pub(crate) struct GameLoadPlugin;

impl Plugin for GameLoadPlugin {
    fn build(&self, app: &mut App) {
        // TODO
    }
}
